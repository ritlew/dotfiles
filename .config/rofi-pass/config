# menu, autotype, copyPass, typeUser, typePass, copyUser, copyUrl, viewEntry, typeMenu, actionMenu, copyMenu, openUrl
default_do='copyPass' 
clip=clipboard
clip_clear=20

_rofi () {
    rofi -i -no-auto-select -theme breeze-dark "$@"
}

